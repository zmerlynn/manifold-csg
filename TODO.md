# TODO

## Safe wrapper gaps (low priority)

- [ ] MeshGL/MeshGL64 construction with full options (`w_options` — merge vectors + run data)

## Ergonomics (nice-to-have)

- [ ] Extract vec-building helper to reduce boilerplate in batch operations (`batch_boolean`, `batch_hull`, `compose` in both Manifold and CrossSection)
